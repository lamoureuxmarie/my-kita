<%= simple_form_for [bookmark, reminder] do |f| %>
  <%= f.input :title %>
  <%= f.input :content, as: :text %>
  <%= f.input :due_date, as: :string, input_html: {class: "datepicker"} %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
